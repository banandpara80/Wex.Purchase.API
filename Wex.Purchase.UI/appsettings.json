{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ApiAuthentication": {
    "ApiKey": "secret",
    "BaseUrl": "http://host.docker.internal:8080/"
    //"BaseUrl": "http://localhost:5271/" Use "localhost" when running locally, as the application will be running on the host machine and needs to connect to the API running on the same machine.
  }
}
